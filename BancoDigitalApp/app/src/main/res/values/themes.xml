<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BancoDigitalApp" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@android:color/white</item>
        <item name="android:navigationBarColor">@color/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Base theme2 with light status bar. -->
    <style name="Theme.BancoDigitalApp2" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
    <!-- THEME EditText -->
    <style name="Theme.EditText" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@android:color/white</item>
    </style>
    <!-- THEME SPLASH -->
    <style name="Theme.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@android:color/white</item>
        <item name="colorAccent">@android:color/white</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- THEME ALERT DIALOG -->
    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/bg_alert_dialog</item>
        <item name="windowMinWidthMinor">80%</item>
    </style>
    <!-- THEME PROGRESSBAR -->
    <style name="Theme.ProgressBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@android:color/white</item>
    </style>
</resources>