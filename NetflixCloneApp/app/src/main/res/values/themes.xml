<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NetflixCloneApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/cor_bg_app</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- THEME Red ProgressBar -->
    <style name="ThemeProgressBar" parent="Theme.NetflixCloneApp">
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <!-- THEME ALERT DIALOG -->
    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/bg_alert_dialog</item>
        <item name="windowMinWidthMinor">80%</item>
    </style>

    <!-- THEME SEARCHVIEW -->
    <style name="SearchViewTheme">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="android:textColorHint">#7C7C7C</item>
        <item name="android:editTextColor">@android:color/white</item>
    </style>

    <!-- THEME FLOAT ACTION BUTTON -->
    <style name="FloatingActionButtonTheme">
        <item name="colorOnSecondary">@android:color/white</item>
    </style>

    <!-- THEME SeekBar -->
    <style name="SeekBarTheme" parent="">
        <item name="android:backgroundTint">@color/colorPrimaryDark</item>
        <item name="android:progressTint">@color/colorPrimary</item>
        <item name="colorSecondary">@color/colorPrimaryDark</item>
    </style>
</resources>